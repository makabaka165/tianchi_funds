# 余额宝申购赎回预测执行建议

## 1. 任务定位

当前赛题不是大模型微调任务，也不是必须使用深度学习的任务。它更适合定位为：

- 金融资金流量预测
- 结构化数据建模
- 时间序列回归预测
- 机器学习特征工程任务

目标是根据 2013-07-01 至 2014-08-31 的历史数据，预测 2014 年 9 月每天的申购总额和赎回总额，最终生成 30 行提交结果。

建议优先使用传统机器学习和时间序列特征方法，例如 LightGBM、XGBoost、随机森林、线性模型、ARIMA/指数平滑等。深度学习可以作为后续实验方向，但不是第一优先级。

## 2. 是否需要创建机器学习环境

建议创建独立的机器学习环境。

你当前电脑已经有 Miniforge，用它来隔离环境完全可以，而且是比较推荐的方式。原因是：

- 不污染系统 Python 环境
- 便于固定依赖，复现实验
- pandas、numpy、scikit-learn、lightgbm 等包可以通过 conda-forge 方便安装
- 后续如果尝试 PyTorch、深度学习模型，也可以另建环境，不影响当前机器学习环境

建议环境名称：

```bash
funds-ml
```

推荐先创建轻量机器学习环境，不需要一开始安装深度学习框架。

## 3. 推荐环境配置

在 Miniforge Prompt 或 PowerShell 中执行：

```bash
conda create -n funds-ml python=3.11 -y
conda activate funds-ml
```

安装基础依赖：

```bash
conda install -c conda-forge pandas numpy scikit-learn matplotlib seaborn jupyterlab ipykernel tqdm pyarrow -y
```

安装建模库：

```bash
conda install -c conda-forge lightgbm xgboost statsmodels -y
```

如果后续想用 notebook：

```bash
python -m ipykernel install --user --name funds-ml --display-name "Python (funds-ml)"
```

建议先不要安装 PyTorch、TensorFlow 这类深度学习框架，除非传统机器学习基线已经跑通并且还想继续提升。

## 4. 推荐项目结构

可以在当前目录下逐步整理为：

```text
E:\tianchi_funds
├── Purchase Redemption Data
│   ├── comp_predict_table.csv
│   ├── mfd_bank_shibor.csv
│   ├── mfd_day_share_interest.csv
│   ├── user_balance_table.csv
│   └── user_profile_table.csv
├── 赛题.md
├── 执行建议.md
├── notebooks
│   └── 01_eda_baseline.ipynb
├── src
│   ├── make_daily_features.py
│   ├── train_baseline.py
│   └── predict_submit.py
├── output
│   └── tc_comp_predict_table.csv
└── models
```

如果只是快速实验，可以先从一个 notebook 开始；如果要形成稳定流程，再拆成 `src` 脚本。

## 5. 数据理解重点

主要数据表：

- `user_balance_table.csv`：核心交易流水，包含每日用户余额、申购、赎回、消费、转出、收益等字段。
- `user_profile_table.csv`：用户性别、城市、星座等静态信息。
- `mfd_day_share_interest.csv`：余额宝收益率。
- `mfd_bank_shibor.csv`：银行间拆借利率。
- `comp_predict_table.csv`：提交样例。

最终预测目标不是单个用户行为，而是每天全量用户的总申购和总赎回。

因此第一步建议将 `user_balance_table.csv` 按 `report_date` 聚合成每日粒度：

- 每日总申购：`sum(total_purchase_amt)`
- 每日总赎回：`sum(total_redeem_amt)`
- 每日总余额：`sum(tBalance)`
- 每日总消费：`sum(consume_amt)`
- 每日总转出：`sum(transfer_amt)`
- 每日总收益：`sum(share_amt)`
- 每日活跃用户数：`nunique(user_id)`

再把收益率表和 Shibor 表按日期合并进来。

## 6. 建模流程建议

### 阶段一：探索性分析

先做 EDA，重点看：

- 申购总额、赎回总额的日趋势
- 周期性：周一到周日是否有明显差异
- 月初、月末是否有明显波动
- 2014 年 8 月和 9 月之间是否可能存在特殊时间效应
- 收益率和申购、赎回之间是否有滞后相关
- Shibor 利率和资金流入流出之间是否有关系

建议先画图：

- 每日申购曲线
- 每日赎回曲线
- 申购/赎回按星期分组箱线图
- 申购/赎回的 7 日滚动均值
- 申购/赎回与收益率、Shibor 的相关性热力图

### 阶段二：构造每日特征

核心特征建议：

- 日期特征：
  - 年、月、日
  - 星期几
  - 是否周末
  - 是否月初
  - 是否月末
  - 距月初第几天
  - 距月末第几天

- 滞后特征：
  - 前 1 天申购、赎回
  - 前 2 天申购、赎回
  - 前 3 天申购、赎回
  - 前 7 天申购、赎回
  - 前 14 天申购、赎回
  - 前 30 天申购、赎回

- 滚动窗口特征：
  - 过去 3 天均值、最大值、最小值、标准差
  - 过去 7 天均值、最大值、最小值、标准差
  - 过去 14 天均值、最大值、最小值、标准差
  - 过去 30 天均值、最大值、最小值、标准差

- 外部金融特征：
  - 万份收益 `mfd_daily_yield`
  - 七日年化收益率 `mfd_7daily_yield`
  - Shibor 各期限利率
  - 收益率和利率的滞后值

- 用户聚合特征：
  - 每日活跃用户数
  - 每日有申购用户数
  - 每日有赎回用户数
  - 人均申购额
  - 人均赎回额

### 阶段三：建立基线模型

先建立简单基线，确认流程完整：

1. 使用最近 7 天均值预测未来同星期。
2. 使用最近 14 天或 30 天滚动均值预测。
3. 使用按星期分组的历史均值预测。

这些基线不一定最高分，但非常重要，因为它们可以帮助判断机器学习模型是否真的有效。

### 阶段四：机器学习模型

建议分别训练两个回归模型：

- 申购模型：预测 `purchase`
- 赎回模型：预测 `redeem`

优先模型：

- LightGBMRegressor
- XGBoostRegressor
- RandomForestRegressor
- Ridge/Lasso 线性模型作为对照

训练方式：

- 训练集：2013-07-01 至 2014-07-31
- 验证集：2014-08-01 至 2014-08-31
- 预测集：2014-09-01 至 2014-09-30

由于最终要预测连续未来 30 天，建议注意避免数据泄漏。预测 2014 年 9 月时，不能使用 9 月真实申购/赎回结果作为滞后特征。可以采用递归预测：先预测 9 月 1 日，再把预测值作为后续日期的滞后输入。

### 阶段五：验证指标

赛题使用相对误差和隐藏得分函数，且申购占 45%，赎回占 55%。

本地验证可以用近似指标：

```text
relative_error = abs(pred - actual) / actual
score_proxy = 0.45 * purchase_relative_error + 0.55 * redeem_relative_error
```

本地目标是让这个加权相对误差尽量小。

注意：赎回权重更高，所以赎回模型的稳定性更重要。

## 7. 推荐执行顺序

建议按照下面顺序推进：

1. 创建 `funds-ml` conda 环境。
2. 读取并检查所有 CSV 文件。
3. 将用户级数据聚合为每日数据。
4. 合并收益率和 Shibor 数据。
5. 做 EDA，观察周期和异常点。
6. 建立滚动均值和星期均值基线。
7. 构造日期、滞后、滚动窗口、金融外部特征。
8. 使用 2014 年 8 月作为验证集。
9. 训练 LightGBM/XGBoost 双目标回归模型。
10. 对 2014 年 9 月递归预测。
11. 生成 `tc_comp_predict_table.csv`。
12. 对提交文件格式做最终检查。

## 8. 第一版代码目标

第一版不要追求复杂，目标是跑通完整闭环：

- 输入原始 CSV
- 输出每日聚合数据
- 训练一个简单模型
- 在 2014 年 8 月做验证
- 预测 2014 年 9 月
- 生成提交文件

第一版可接受模型很简单，例如：

- 按星期均值预测
- 7 日滚动均值预测
- LightGBM 基线模型

跑通之后再迭代特征和模型。

## 9. 后续提升方向

如果基线跑通，可以继续尝试：

- 分别针对申购和赎回调参
- 加强节假日/月初/月末特征
- 增加用户行为聚合特征
- 对大额用户和普通用户分层聚合
- 对申购、赎回分别做异常值处理
- 使用多个模型加权融合
- 使用 2014 年 7 月、8 月做多窗口交叉验证
- 尝试 SARIMA、Prophet 类时间序列模型作为补充
- 最后再考虑 LSTM/GRU/TCN 等深度学习模型

## 10. 当前建议结论

当前最推荐路线：

```text
Miniforge 独立环境 + pandas 特征工程 + LightGBM/XGBoost 回归 + 时间序列验证 + 递归预测提交
```

这个赛题的关键不是模型越深越好，而是：

- 日期和周期特征是否处理好
- 聚合口径是否正确
- 验证方式是否接近真实预测场景
- 未来 30 天预测时是否避免数据泄漏
- 申购和赎回是否分别建模并针对性优化

因此，先创建机器学习环境就足够，不需要直接创建深度学习环境。
