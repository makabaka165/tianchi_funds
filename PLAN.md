# 余额宝申购赎回预测持续执行计划

## 1. 建议持续目标（/goal）

建议当前线程持续使用下面这个目标：

```text
/goal 在保持当前正式候选基线稳定的前提下，降低“本地代理分数与官网真实分数”的偏差，优先提升跨月泛化稳定性、控制赎回误差与坏日率，并且只保留能够严格改善正式候选质量的改动。
```

这个目标不是单纯追求把本地 `weighted_proxy_score_mean` 从 `6.089` 继续小幅拉高，而是要同时满足：

- 提升正式候选质量
- 提升跨月稳定性
- 降低对固定日号规则的依赖
- 减少“本地看起来更好，官网反而不涨”的失真风险

---

## 2. 当前阶段判断

### 2.1 当前有效基线

当前正式候选仍然是 `7a9f211` 对应的规则基线，关键指标：

```text
rolling overall weighted_proxy_score_mean = 6.089217
2014-08 weighted_proxy_score_mean        = 6.516990
2014-08 Decision                         = PASS
overall bad_day_rate_max                 = 0.203252
worst month                              = 2014-06
```

### 2.2 当前代码结构结论

根据当前 `src/baseline_weekday_mean.py` 和已有实验记录，现状可以归纳为：

1. 当前基线已经不是“简单星期均值”，而是“星期均值 + 校准 + 节假日 + 多层固定规则”的复合规则模型。
2. 已经做过大量单规则加法实验和两轮减法审计，近期可见的低风险单规则空间基本已经耗尽。
3. 已经验证过的简单 GBDT challenger、残差修正、保守融合都没有胜过当前正式基线。
4. 本地线性 `proxy_score` 只是近似排序工具，不能直接对应官网真实分数。

### 2.3 当前核心问题

当前阶段真正的问题不是“还差哪一条单日规则”，而是下面这两个：

1. **泛化问题**  
   本地 rolling 指标已经不低，但官网已知成绩只有约 `97`，说明当前优化方向和真实测试月的得分逻辑之间存在偏差。

2. **结构问题**  
   当前规则模型在 `predict_target()` 中已经累计使用大量固定日号/星期/区间规则，解释性虽然强，但过拟合风险也明显增大。

---

## 3. 总体目标与结束条件

## 3.1 总体目标

下一阶段的总目标定义为：

```text
在不破坏当前正式候选的前提下，建立一套以“泛化稳定性优先”为核心的持续实验机制，
逐步减少对局部规则堆叠的依赖，
找到至少一个比当前正式基线更值得提交官网的候选版本。
```

## 3.2 目标完成的硬条件

只有同时满足以下条件，才视为本阶段目标“完成”：

1. 找到一个新候选，能够替换当前正式基线。
2. 该候选在本地 8 月验证中仍然 `PASS`。
3. 该候选在 5-8 月 rolling 上至少满足以下全部约束：
   - `overall weighted_relative_error_mean` 明显低于当前基线
   - 或 `2014-06` 明显改善且整体不退化
   - `overall bad_day_rate_max` 不高于当前基线
   - 单月最大退化不超过既定风险线
4. 该候选不是靠继续新增大量固定日号补丁获得的。
5. 该候选经过一次官网提交后，真实线上分数相对上一正式版本有正向提升。

## 3.3 阶段终止条件

出现以下任一情况，本阶段应视为“暂时结束，需要换战略”：

1. 连续两轮 challenger 路线都明显不如当前规则基线。
2. 继续实验只能带来极小的本地代理改善，但无法降低结构性过拟合风险。
3. 新候选在本地指标上提升，但官网再次没有提升，说明当前本地验证机制仍有明显偏差。
4. 官网剩余提交次数过少（例如仅剩 1 次），不足以承担继续线上试错的风险；此时应冻结当前正式候选与线下阶段结论，暂停推进，直到提交预算恢复或出现新的高置信度外部信息。

---

## 4. 持续执行约束

## 4.1 保留 / 回退总规则

后续所有实验统一遵循：

```text
提升才保留
不提升就回退代码和正式输出
但实验记录必须保留
```

具体执行规则：

1. 任何实验如果不能替换正式候选，则：
   - 回退实验代码
   - 回退 README / 正式输出改动
   - 只保留 `操作记录.md`
2. 任何实验如果只是“研究资产”，必须提前明确：
   - 是否允许保留脚本
   - 是否允许保留报告
   - 默认不允许，除非用户单独批准
3. 正式提交文件 `output/tc_comp_predict_table.csv` 只能由当前最佳正式候选持有。

## 4.2 正式候选保留门槛

只有同时满足以下条件，才允许替换正式候选：

1. `src/evaluate.py` 对 8 月验证结果为 `PASS`
2. 提交文件格式无误
3. 与当前正式基线相比满足以下之一：
   - `overall weighted_relative_error_mean` 更低
   - `2014-06 weighted_relative_error_mean` 明显更低且 overall 不退化
   - `overall bad_day_rate_max` 更低
4. 单月最大风险退化不超过 `-0.05`
5. 新方案的结构风险不高于当前基线，或者虽然更复杂，但能带来明确泛化收益

## 4.3 记录要求

每轮实验都必须在 `操作记录.md` 写清楚：

- 目标
- 改动内容
- 为什么做
- 8 月评价
- 5-8 月 rolling 结果
- 相对当前正式基线的 delta
- 是否保留
- 是否回退
- 对 9 月真实提交日期的影响

---

## 5. 下一阶段的工作主线

## 5.1 主线 A：修正本地验证的使用方式

目标不是重写 `evaluate.py`，而是修正使用方式：

1. 保留当前线性 `proxy_score`
2. 明确把它降级为“排序参考指标”
3. 后续排序优先级改为：
   - 第一优先：`overall weighted_relative_error_mean`
   - 第二优先：`2014-06` 改善程度
   - 第三优先：`overall bad_day_rate_max`
   - 第四优先：月度波动是否更均衡
   - 第五优先：`weighted_proxy_score_mean`

也就是说，后续不允许再出现“proxy 稍涨就保留，但 error、风险和结构都更差”的情况。

## 5.2 主线 B：停止单点补丁式提分

以下方向默认暂停，不再作为主线：

- 新增单日规则
- 新增星期单点因子
- 在 `baseline_weekday_mean.py` 继续堆更多乘法补丁
- 围绕已审计过的规则反复做相邻系数微调

原因：

1. 已有实验已经说明这条路线边际收益极低
2. 当前规则复杂度已经很高
3. 这条路线更容易抬高本地 proxy，却不一定提高官网成绩

## 5.3 主线 C：转向“结构性 challenger”

后续 challenger 必须满足两个要求：

1. **结构上比当前规则基线更稳**
2. **评价上至少不弱于当前规则基线**

优先顺序：

### 第一优先：规则基线增强型残差模型（更稳的第二轮）

不是直接预测总量，而是：

```text
以当前规则基线输出为先验
学习 residual 或乘法修正
但严格控制残差模型的波动范围
```

这条线与之前失败的残差 challenger 的区别是：

- 不允许无约束残差放大
- 要加入截断/裁剪/稳定化策略
- 要先针对 `redeem` 单独做低方差修正

### 第二优先：分目标 challenger

申购和赎回分开处理，不共享实验策略：

- `purchase`：偏向低方差、平滑的时间序列特征
- `redeem`：重点解决最差月和坏日率

### 第三优先：结构减法版本的规则基线

如果 challenger 继续失败，则反过来做“结构压缩”实验：

- 不是删单条规则
- 而是把相近规则合并成更少的窗口逻辑
- 比较“更少规则、更低复杂度”是否能维持接近分数但减少过拟合风险

---

## 6. 具体执行步骤

## 6.1 Step 1：建立新的正式比较基准

先固定一个新的“正式比较表”，每次实验统一比较：

```text
baseline overall weighted_relative_error_mean
baseline overall weighted_proxy_score_mean
baseline overall bad_day_rate_max
baseline 2014-06 weighted_relative_error_mean
baseline 2014-06 weighted_proxy_score_mean
baseline month-by-month std / 波动情况
```

这一步的意义是：后续所有实验都不能只拿 proxy 做比较。

## 6.2 Step 2：做第二轮稳定化残差 challenger

执行要求：

1. 仍然复用 `build_daily_features()`
2. 仍然以当前规则模型为 baseline
3. 只学习残差，但要增加：
   - 残差裁剪
   - 日级最大相对修正幅度限制
   - 只在高置信特征区间启用修正
4. 先只做 `redeem` 残差修正
5. `purchase` 先保持规则基线不动

保留条件：

- 至少改善 `2014-06`
- 不恶化 `overall bad_day_rate_max`
- 8 月仍然 `PASS`

## 6.3 Step 3：做低复杂度融合 challenger

如果 Step 2 失败，则尝试更保守的融合：

1. 不用纯 ML 直接替代规则基线
2. 只允许小权重 challenger 参与：
   - `rule 0.95 + challenger 0.05`
   - `rule 0.90 + challenger 0.10`
   - `rule 0.85 + challenger 0.15`
3. 只保留能改善 error 和风险的组合

重点：

- 不再把整体 proxy 提升当作充分条件
- 若 2014-06 更差、bad_day_rate 更差，则直接淘汰

## 6.4 Step 4：做结构压缩审计

如果 Step 2 和 Step 3 都失败，则进入结构压缩：

1. 不做“删一条规则”的细粒度审计
2. 改做“合并一组相邻规则”的实验
3. 优先合并：
   - 同一日期附近的多层赎回规则
   - 同一 weekday 上重复叠加的逻辑
   - 月末窗口中功能相近的规则

保留目标：

- 分数接近当前基线
- 复杂度显著下降
- 结构风险降低

---

## 7. 每轮实验标准流程

每一轮都按下面顺序执行：

1. 从当前正式候选出发
2. 新建或修改实验脚本
3. 生成验证结果和 9 月提交文件
4. 运行：

```powershell
conda run -n funds-ml python src\baseline_weekday_mean.py
conda run -n funds-ml python src\evaluate.py
conda run -n funds-ml python src\rolling_validate.py
```

如果是 challenger，则补充运行 challenger 自己的验证脚本和 8 月评价脚本。

5. 与正式基线逐项比较：

```text
8 月 weighted_relative_error_mean
8 月 weighted_proxy_score_mean
8 月 bad_day_rate_max
2014-05 / 06 / 07 / 08 各月 weighted_relative_error_mean
2014-05 / 06 / 07 / 08 各月 weighted_proxy_score_mean
overall weighted_relative_error_mean
overall weighted_proxy_score_mean
overall bad_day_rate_max
worst month
```

6. 判断：
   - 满足保留门槛：更新正式候选
   - 不满足：回退代码和正式输出，只保留记录

---

## 8. PLAN 的完成标记

以下条件满足时，可以认为本计划已阶段性完成：

1. 当前正式候选被一个新版本替换
2. 新版本不是依赖更多固定日号补丁取得提升
3. 新版本在 `2014-06` 或整体 error 上有明确改善
4. 新版本本地验证通过后，官网提交结果也优于上一正式候选

如果没有满足上述条件，但已经完成：

- 两轮 challenger
- 一轮结构压缩实验
- 一轮重新校准验证机制

且仍未出现更优候选，则应把当前计划标记为“验证完成，进入下一战略阶段”，而不是继续在同一条路线里重复微调。

---

## 9. 当前默认执行结论

基于当前仓库状态，接下来最合理的执行顺序是：

```text
先做更稳的 redeem-only 残差修正 challenger
如果失败，再做低权重融合 challenger
如果仍失败，再做规则结构压缩
```

不建议的路线：

```text
继续加单日规则
继续围绕已失败的规则做相邻系数回试
继续把线性 proxy 当作唯一优化目标
```
